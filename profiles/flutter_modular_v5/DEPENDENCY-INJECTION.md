# Dependency Injection: Flutter Modular v5

Declare dependencies in `List<Bind<Object>> binds` with `Bind.factory`, `Bind.lazySingleton`, or the established bind lifetime. Resolve through the v5 injector/`Modular.get<T>()`. Keep route, service, repository, and Cubit binds aligned with the existing application module.
