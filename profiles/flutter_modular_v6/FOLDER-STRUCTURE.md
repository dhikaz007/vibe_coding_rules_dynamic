# Folder Structure: Flutter Modular v6

Follow the existing feature shape: `feature/<name>/config/` for module, routes, and bind files; `cubit/`, `domain/`, and `screens/` for the feature implementation. Shared app routes live in `lib/routes/`; common infrastructure follows the existing `services/`, `shared/`, helpers, and storage structure.
